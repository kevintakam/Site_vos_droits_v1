<!DOCTYPE html>
<html lang="fr">
  <body>
    <p>Bonjour {{ user.firstname ?? '' }} {{ user.lastname ?? '' }},</p>

    <p>Merci de votre inscription. Pour activer votre compte, cliquez :</p>

    <p>
      <a href="{{ signedUrl }}" style="display:inline-block;padding:10px 16px;border-radius:6px;background:#2d6cdf;color:#fff;text-decoration:none;">
        Vérifier mon e-mail
      </a>
    </p>

    <p>Si le bouton ne fonctionne pas, copiez/collez ce lien :</p>
    <p style="word-break: break-all;"><a href="{{ signedUrl }}">{{ signedUrl }}</a></p>

    {% if expiresAtMessageKey is defined %}
      <p style="color:#666;font-size:12px;">
        Ce lien expirera : {{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}.
      </p>
    {% endif %}
  </body>
</html>
